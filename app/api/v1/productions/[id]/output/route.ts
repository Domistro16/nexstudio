import path from "node:path";
import { requireSession } from "@/lib/route-auth";
import { getPrisma } from "@/lib/db";
import { problem } from "@/lib/http";
import { readObject } from "@/lib/object-storage";
export const runtime="nodejs";
export async function GET(request:Request,context:{params:Promise<{id:string}>}){const auth=await requireSession(request);if(auth.response)return auth.response;const{id}=await context.params;const url=new URL(request.url);const requestedVersion=Number(url.searchParams.get("version")||0);const production=await getPrisma()!.production.findFirst({where:{id,ownerUserId:auth.session!.userId},include:{currentVersion:true}});if(!production)return problem(auth.id,404,"PRODUCTION_OUTPUT_NOT_FOUND","Production output not found","This production does not have a finished output yet.");const version=requestedVersion>0?await getPrisma()!.productionVersion.findFirst({where:{productionId:id,versionNumber:requestedVersion}}):production.currentVersion;if(!version?.outputObjectKey)return problem(auth.id,404,"PRODUCTION_OUTPUT_NOT_FOUND","Production output not found","This version does not have a finished output yet.");const bytes=await readObject(version.outputObjectKey).catch(()=>null);if(!bytes)return problem(auth.id,404,"PRODUCTION_OUTPUT_MISSING","Production output is unavailable","The persisted film could not be read.");const inline=url.searchParams.get("disposition")==="inline";const extension=path.extname(version.outputObjectKey)||".mp4";const filename=`${production.title.replace(/[^a-z0-9_-]+/gi,"-").replace(/^-|-$/g,"")||"studio-film"}-v${version.versionNumber}${extension}`;return new Response(bytes,{headers:{"content-type":"video/mp4","content-length":String(bytes.byteLength),"content-disposition":`${inline?"inline":"attachment"}; filename*=UTF-8''${encodeURIComponent(filename)}`,"cache-control":"private, no-store","x-content-type-options":"nosniff","x-studio-output":"verified-production","x-studio-version":String(version.versionNumber)}})}
