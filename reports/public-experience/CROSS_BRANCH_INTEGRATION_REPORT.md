# STUDIO PUBLIC 10/10 — Cross-branch integration report

## Base and authority ordering

The final visible branch starts from the recovered Public Experience integrated source, then reconciles later customer-facing authorities without allowing older full-source archives to roll back the face.

- Authenticated Production Desk additions were merged: current Series, Brand, Assets/Files, Cast, production-memory controls, owner-scoped preview/poster delivery and corrected memory-domain test runner.
- Trust/Commerce additions were merged: real payment-return reconciliation, exact-production low-balance funding, payment-pending continuation, upload/media security and account/auth infrastructure.
- The Public Experience `ProductionWorkspace` remains the visible authority; older workspace files from those patches are not allowed to overwrite keyboard focus, version UX or the no-AI-theatre design.
- The public registry retains exactly four production families; Series is continuity scope, not a fifth family.

## Defects found and fixed during this branch

1. Shipped workspace still contained an orbiting planning element/live status dot despite an older report claiming those were removed. Final source removes them.
2. Authenticated production-memory control had no Public Experience styling. It now uses the same calm responsive system and has browser evidence.
3. Trust asset route initially removed the Authenticated Desk's real media preview URL. Final route exposes only owner-scoped CLEAN/READY reusable image/video content.
4. Trust QA falsely classified the server import `node:crypto` as customer crypto billing language. The audit now excludes module specifiers and still checks customer-visible copy.
5. Memory-domain test runner referenced stale `/mnt/data/studio-memory-work`; the later self-contained runner is restored.

## Creative-authority boundary

The later P8 V3 bounded-autonomous-repair patch is deliberately **not** blindly overlaid here. Its package targets the clean P8 V2 base and would replace newer Studio memory/calibration wrapper contracts. This branch does not rewrite NexMind to make a UI package look more current. Final product integration should apply the creative-authority branch through its own controlled merge/revalidation path.
